node_modules/
dist/
out/
*.log
.DS_Store
target/
.mcp.json
.preview/
release/
# Bare pattern: matches at any depth, so the README hero needs an explicit negation.
screenshot.png
!docs/screenshot.png
scripts/canvas-test/harness.bundle.mjs
# Written beside the golden when a visual test fails; a diff aid, never committed.
*.actual.png
# Regenerated by tools/render-mock-svg.mjs; the shipped copy lives in the website repo.
mock-canvas.svg
